ybc_deps:
  - yast2

ruby_deps:
  - packager

moves:
  - from: "src/dialogs/OUCDialogs.ycp"
    to:   src/include/online-update-configuration

excluded:
  # TODO: Include file, can't be compiled for now.
  - src/include/online-update-configuration/OUCDialogs.ycp
