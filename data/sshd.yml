deps:
  - testsuite
  - yast2

moves:
  - from: "src/[A-Z]*.ycp"
    to: src/modules
  - from: "src/sshd*.ycp"
    to: src/clients
  - from: "src/*.ycp"
    to: src/include/sshd
  - from: "src/*.rnc"
    to: src/autoyast-rnc
  - from: "src/*.desktop"
    to: src/desktop
