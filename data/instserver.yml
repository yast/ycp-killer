deps:
  - yast2
  - slp

moves:
  - from: "src/[A-Z]*.ycp"
    to: src/modules
  - from: "src/instserver.ycp"
    to: src/clients
  - from: "src/*.ycp"
    to: src/include/instserver
  - from: "src/*.desktop"
    to: src/desktop
  - from: "src/httpd"
    to: "."
