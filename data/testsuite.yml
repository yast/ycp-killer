files:
  - prefix: src
    # Modules (in Makefile.am order)
    modules:
    - Testsuite.ycp
    - Pkg.ycp
  # Includes (in Makefile.am order)
    include:
    - testsuite.ycp
  - prefix: testsuite
  # Testsuite (in alphabetical order)
    testsuite:
    - test.ycp
