deps:
  - yast2
  - storage

moves:
  - from: "src/Reipl.ycp"
    to:   src/modules
  - from: "src/{reipl,inst}*.ycp"
    to:   src/clients
  - from: "src/{complex,dialogs,helps,wizards}.ycp"
    to:   src/include/reipl
  - from: "src/reipl.desktop"
    to:   src/reipl

excluded:
  # TODO: Include file, can't be compiled for now.
  - src/include/reipl/dialogs.ycp
